celery==5.4.0
redis==5.2.1
requests==2.32.3
# NLP stack (no torch, no sentence-transformers)
spacy>=3.7
stanza>=1.8
# Embedding client (sync HTTP to TEI)
numpy>=1.26
# Segmentation math
hnswlib>=0.8
scipy>=1.11
scikit-learn>=1.3
rapidfuzz>=3.0
# Tokenizer (lightweight BPE, no model weights beyond vocab)
tokenizers>=0.19
