{
  "type": "object",
  "additionalProperties": false,
  "required": ["patterns", "incidents", "document_signals"],
  "properties": {
    "patterns": {
      "type": "array",
      "description": "Códigos em {label_name} que nomeiam processos comportamentais recorrentes. Cada padrão é evidenciado por múltiplos segmentos.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "pattern_name",
          "definition",
          "segment_refs",
          "confidence"
        ],
        "properties": {
          "pattern_name": {
            "type": "string",
            "description": "{label_desc}"
          },
          "definition": {
            "type": "string",
            "description": "2-3 frases definindo o que este padrão captura: qual comportamento se repete, sob quais condições, com qual variação."
          },
          "segment_refs": {
            "type": "array",
            "items": { "type": "integer" },
            "description": "Índices base 1 dos segmentos que evidenciam este padrão."
          },
          "confidence": {
            "type": "string",
            "enum": ["HIGH", "MEDIUM", "LOW"],
            "description": "Com que força os segmentos convergem neste padrão."
          }
        }
      }
    },
    "incidents": {
      "type": "array",
      "description": "Frases curtas de evidência. Coisas concretas que o participante fez, disse, sentiu ou vivenciou.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["description", "segment_refs"],
        "properties": {
          "description": {
            "type": "string",
            "description": "Frase curta descrevendo o que aconteceu. Linguagem simples, {label_anti}."
          },
          "segment_refs": {
            "type": "array",
            "items": { "type": "integer" },
            "description": "Índices base 1 dos segmentos de onde este incidente se origina."
          },
          "patterns": {
            "type": "array",
            "items": { "type": "integer" },
            "description": "Índices base 0 dos padrões que este incidente apoia. Opcional."
          },
          "exact_quote": {
            "type": "string",
            "description": "Citação literal do segmento que melhor captura este incidente. Opcional."
          }
        }
      }
    },
    "document_signals": {
      "type": "object",
      "additionalProperties": false,
      "required": ["core_pattern", "tentative_concern"],
      "properties": {
        "core_pattern": {
          "type": "string",
          "description": "Qual padrão central de {object_of_study} impulsiona o comportamento deste participante? 2-3 frases."
        },
        "core_pattern_confidence": {
          "type": "string",
          "enum": ["HIGH", "MEDIUM", "LOW"],
          "description": "Confiança na avaliação do padrão central de {object_of_study}."
        },
        "tentative_concern": {
          "type": "string",
          "description": "O que este participante está continuamente tentando resolver? Uma frase em {label_name}."
        },
        "anomalies": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Segmentos ou padrões que não se encaixam ou exigem verificação. Opcional."
        },
        "needs_verification": {
          "type": "boolean",
          "description": "true se este documento contém padrões que devem ser sinalizados para verificação."
        }
      }
    }
  }
}
