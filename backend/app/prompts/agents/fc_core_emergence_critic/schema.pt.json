{
  "type": "object",
  "properties": {
    "verdicts": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["candidate_label", "verdict", "rationale"],
        "properties": {
          "candidate_label": {
            "type": "string",
            "description": "Rótulo EXATO da categoria como aparece no contexto do proposer (corresponde a category_label do proposer)."
          },
          "code_id": {
            "type": "string",
            "description": "UUID da categoria (opcional — preencher se conhecido do contexto, caso contrário omitir)."
          },
          "verdict": {
            "type": "string",
            "enum": ["valid", "refine", "split"],
            "description": "Veredito de intercambialidade"
          },
          "rationale": {
            "type": "string",
            "description": "Justificativa citando incidentes específicos de documentos diferentes. Deve indicar quais incidentes foram comparados."
          },
          "interchangeable_pairs": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "incident_a_doc": {
                  "type": "string"
                },
                "incident_b_doc": {
                  "type": "string"
                },
                "why_interchangeable": {
                  "type": "string"
                }
              }
            },
            "description": "Pares de incidentes de DOCUMENTOS DIFERENTES que são claramente intercambiáveis"
          },
          "non_interchangeable_pairs": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "incident_a_doc": {
                  "type": "string"
                },
                "incident_b_doc": {
                  "type": "string"
                },
                "why_different": {
                  "type": "string"
                }
              }
            },
            "description": "Pares de documentos diferentes que revelam padrões distintos"
          },
          "suggested_action_if_not_valid": {
            "type": "string",
            "description": "Ação concreta: refinar definição, dividir em subcódigos ou buscar mais dados?"
          }
        }
      }
    }
  },
  "required": ["verdicts"]
}
