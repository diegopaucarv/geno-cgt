{
  "type": "object",
  "required": ["observations", "suggestions"],
  "properties": {
    "observations": {
      "type": "array",
      "description": "Qualitative Beobachtungen zu den Kandidaten. Keine Urteile — nur Feedback.",
      "items": {
        "type": "object",
        "required": ["candidate_index", "observation_type", "detail"],
        "properties": {
          "candidate_index": {
            "type": "integer",
            "description": "0-basierter Index des Kandidaten aus dem candidates-Array des Proposers."
          },
          "observation_type": {
            "type": "string",
            "description": "Art der Beobachtung: grounding, coverage, abstraction, tension_vs_theme, supporting_evidence."
          },
          "detail": {
            "type": "string",
            "description": "Spezifische Beobachtung. Was ist Ihnen aufgefallen? Evidenz zitieren."
          },
          "is_strong": {
            "type": "boolean",
            "description": "true wenn dieser Kandidat eine starke empirische Verankerung in den Daten hat."
          }
        }
      }
    },
    "suggestions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Umsetzbare Vorschläge für den Forscher. Was bei der Auswahl zu bedenken ist."
    },
    "referenced_entities": {
      "type": "object",
      "description": "Entities from the database that were referenced in this analysis. Populated by the orchestration layer for traceability.",
      "properties": {
        "segment_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Segment IDs referenced in this output."
        },
        "code_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Category/code IDs referenced in this output."
        },
        "hypothesis_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Hypothesis IDs referenced in this output."
        },
        "incident_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Incident IDs referenced in this output."
        },
        "document_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Document IDs referenced in this output."
        }
      }
    }
  }
}
