{
  "type": "object",
  "required": ["observations", "suggestions"],
  "properties": {
    "observations": {
      "type": "array",
      "description": "Observações qualitativas sobre os candidatos. Sem vereditos — apenas feedback.",
      "items": {
        "type": "object",
        "required": ["candidate_index", "observation_type", "detail"],
        "properties": {
          "candidate_index": {
            "type": "integer",
            "description": "Índice base 0 do candidato do array de candidatos do proponente."
          },
          "observation_type": {
            "type": "string",
            "description": "Tipo de observação: grounding, coverage, abstraction, tension_vs_theme, supporting_evidence."
          },
          "detail": {
            "type": "string",
            "description": "Observação específica. O que você notou? Citar evidência."
          },
          "is_strong": {
            "type": "boolean",
            "description": "true se este candidato tem forte respaldo empírico nos dados."
          }
        }
      }
    },
    "suggestions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Sugestões acionáveis para o pesquisador. O que considerar ao escolher."
    }
  }
}
