{
  "type": "object",
  "required": ["observations", "suggestions"],
  "properties": {
    "observations": {
      "type": "array",
      "description": "Observaciones cualitativas sobre los candidatos. Sin veredictos — solo retroalimentación.",
      "items": {
        "type": "object",
        "required": ["candidate_index", "observation_type", "detail"],
        "properties": {
          "candidate_index": {
            "type": "integer",
            "description": "Índice base 0 del candidato del array de candidatos del proponente."
          },
          "observation_type": {
            "type": "string",
            "description": "Tipo de observación: grounding, coverage, abstraction, tension_vs_theme, supporting_evidence."
          },
          "detail": {
            "type": "string",
            "description": "Observación específica. ¿Qué notaste? Citar evidencia."
          },
          "is_strong": {
            "type": "boolean",
            "description": "true si este candidato tiene un fuerte respaldo empírico en los datos."
          }
        }
      }
    },
    "suggestions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Sugerencias accionables para el investigador. Qué considerar al elegir."
    }
  }
}
