{
  "type": "json_schema",
  "json_schema": {
    "name": "population_generalizer",
    "schema": {
      "type": "object",
      "properties": {
        "generalized_population": {
          "type": "string",
          "description": "População generalizada com escopo teórico. 1-2 frases."
        },
        "spatial_frame": {
          "type": "string",
          "enum": [
            "cohabiting_group",
            "sparse",
            "high_diversity"
          ]
        },
        "temporal_frame": {
          "type": "string",
          "enum": [
            "present_continuous",
            "retrospective",
            "prospective",
            "longitudinal"
          ]
        },
        "confidence": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Confiança na generalização (0.0-1.0)"
        },
        "rationale": {
          "type": "string",
          "description": "Justificativa breve da generalização (2-3 frases)"
        }
      },
      "required": [
        "generalized_population",
        "spatial_frame",
        "temporal_frame",
        "confidence",
        "rationale"
      ]
    }
  }
}
