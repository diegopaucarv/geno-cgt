{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "found_incidents",
    "gradient_expanded"
  ],
  "properties": {
    "found_incidents": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "segment_id",
          "document_name",
          "exact_quote"
        ],
        "properties": {
          "segment_id": {
            "type": "string"
          },
          "document_name": {
            "type": "string"
          },
          "exact_quote": {
            "type": "string"
          },
          "extreme_manifested": {
            "type": "string",
            "enum": [
              "confirms_known_extreme",
              "expands_extreme_further",
              "reveals_new_extreme"
            ],
            "description": "Bestätigt dieser Incident das bekannte Extrem, treibt er es weiter oder offenbart er ein neues Extrem?"
          },
          "elaboration": {
            "type": "string"
          }
        }
      }
    },
    "gradient_expanded": {
      "type": "boolean",
      "description": "true wenn ein Incident den Gradienten über das Bekannte hinaus erweitert hat."
    },
    "expanded_gradient_description": {
      "type": "string",
      "description": "Neuer Gradientenbereich, falls erweitert."
    },
    "corpus_gap": {
      "type": "boolean",
      "description": "true wenn das aktuelle Korpus KEINE Incidente am gesuchten Extrem enthält."
    },
    "sampling_recommendation": {
      "type": "string",
      "description": "Wenn corpus_gap=true: welche Art von Fall zu suchen ist."
    },
    "suggested_interview_question": {
      "type": "string",
      "description": "Konkrete Frage für ein theoretisches Sampling-Dokument."
    }
  }
}
