{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "found_incidents",
    "gradient_expanded"
  ],
  "properties": {
    "found_incidents": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "segment_id",
          "document_name",
          "exact_quote"
        ],
        "properties": {
          "segment_id": {
            "type": "string"
          },
          "document_name": {
            "type": "string"
          },
          "exact_quote": {
            "type": "string"
          },
          "extreme_manifested": {
            "type": "string",
            "enum": [
              "confirms_known_extreme",
              "expands_extreme_further",
              "reveals_new_extreme"
            ],
            "description": "Este incidente confirma o extremo conhecido, o empurra mais além ou revela um novo extremo?"
          },
          "elaboration": {
            "type": "string"
          }
        }
      }
    },
    "gradient_expanded": {
      "type": "boolean",
      "description": "true se algum incidente expandiu o gradiente além do que era conhecido."
    },
    "expanded_gradient_description": {
      "type": "string",
      "description": "Nova faixa de gradiente se expandido."
    },
    "corpus_gap": {
      "type": "boolean",
      "description": "true se o corpus atual NÃO contém incidentes no extremo buscado."
    },
    "sampling_recommendation": {
      "type": "string",
      "description": "Se corpus_gap=true: que tipo de caso procurar."
    },
    "suggested_interview_question": {
      "type": "string",
      "description": "Pergunta concreta para um documento de amostragem teórica."
    }
  }
}
