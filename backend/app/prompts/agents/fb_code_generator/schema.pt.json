{
  "type": "object",
  "required": ["codes", "_self_evaluation"],
  "properties": {
    "codes": {
      "type": "array",
      "description": "Códigos gerados a partir dos indicadores.",
      "items": {
        "type": "object",
        "required": ["code_name", "definition", "relationship_to_existing"],
        "properties": {
          "code_name": {
            "type": "string",
            "description": "Rótulo do código."
          },
          "definition": {
            "type": "string",
            "description": "Definição: qual padrão comportamental captura, em 1-2 frases."
          },
          "indicators": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Indicadores que respaldam este código."
          },
          "variations": {
            "type": "string",
            "description": "Variações internas observadas (graus, matizes, contextos)."
          },
          "relationship_to_existing": {
            "type": "string",
            "description": "Relação com códigos existentes: 'Novo', 'Subcódigo de X', 'Sobreposição com Y'."
          }
        }
      }
    },
    "referenced_entities": {
      "type": "object",
      "description": "Entities from the database that were referenced in this analysis. Populated by the orchestration layer for traceability.",
      "properties": {
        "segment_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Segment IDs referenced in this output."
        },
        "code_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Category/code IDs referenced in this output."
        },
        "hypothesis_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Hypothesis IDs referenced in this output."
        },
        "incident_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Incident IDs referenced in this output."
        },
        "document_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Document IDs referenced in this output."
        }
      }
    },
    "_self_evaluation": {
      "type": "object",
      "properties": {
        "needs_retry": {
          "type": "boolean",
          "description": "true se você considera que sua saída está incompleta, é de baixa qualidade ou requer refinamento adicional"
        },
        "retry_reason": {
          "type": ["string", "null"],
          "description": "APENAS se needs_retry=true. Instrução CONCRETA e ACIONÁVEL sobre o que melhorar na próxima tentativa. Ex.: 'Rótulo muito genérico — especificar propriedades distintivas', 'Falta evidência do segmento 47', 'Definição mistura dois conceitos diferentes — separar'"
        },
        "suggested_action": {
          "type": "string",
          "enum": ["proceed", "retry", "escalate_to_hitl", "skip", "abort"],
          "description": "Ação recomendada: proceed (saída correta, avançar), retry (repetir com feedback), escalate_to_hitl (decisão humana necessária), skip (pular este agente, não é relevante), abort (erro irrecuperável, parar tudo)"
        }
      },
      "required": ["needs_retry", "suggested_action"]
    }
  }
}
