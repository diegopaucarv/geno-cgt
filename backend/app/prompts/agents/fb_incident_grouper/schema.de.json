{
  "type": "object",
  "additionalProperties": false,
  "required": ["groups", "_self_evaluation"],
  "properties": {
    "groups": {
      "type": "array",
      "description": "Vorgeschlagene Gruppen von Vorfällen, die dasselbe Verhaltensmuster belegen",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["signal", "incident_ids", "rationale"],
        "properties": {
          "signal": {
            "type": "string",
            "description": "Ein kurzer Satz, der erfasst, was all diese Vorfälle auf Verhaltensebene gemeinsam haben — das ZUGRUNDELIEGENDE MUSTER, auf das diese verschiedenen Ausdrücke hinweisen. KEIN {label_name}, nur eine beschreibende Bezeichnung. Z.B.: 'verlängerte Arbeitszeiten', 'Vorführung für Bewerter'"
          },
          "incident_ids": {
            "type": "array",
            "items": {"type": "string"},
            "description": "EXAKTE Vorfall-IDs aus der Eingabe, die dieses Muster belegen. Mindestens 2."
          },
          "rationale": {
            "type": "string",
            "description": "Ein Satz, der den ZUGRUNDELIEGENDEN VERHALTENSPROZESS erklärt, der diese Vorfälle trotz ihrer oberflächlichen Unterschiede verbindet"
          }
        }
      }
    },
    "_self_evaluation": {
      "type": "object",
      "properties": {
        "needs_retry": {
          "type": "boolean",
          "description": "true, wenn du der Meinung bist, dass deine Ausgabe unvollständig, von geringer Qualität ist oder zusätzliche Verfeinerung benötigt"
        },
        "retry_reason": {
          "type": ["string", "null"],
          "description": "NUR wenn needs_retry=true. KONKRETE und UMSETZBARE Anweisung, was beim nächsten Versuch verbessert werden soll. Z.B.: 'Label zu allgemein — spezifische Eigenschaften angeben', 'Fehlende Evidenz aus Segment 47', 'Definition vermischt zwei verschiedene Konzepte — trennen'"
        },
        "suggested_action": {
          "type": "string",
          "enum": ["proceed", "retry", "escalate_to_hitl", "skip", "abort"],
          "description": "Empfohlene Aktion: proceed (Ausgabe korrekt, fortfahren), retry (mit Feedback wiederholen), escalate_to_hitl (menschliche Entscheidung erforderlich), skip (diesen Agenten überspringen, nicht relevant), abort (nicht behebbarer Fehler, alles stoppen)"
        }
      },
      "required": ["needs_retry", "suggested_action"]
    }
  }
}
