{
  "type": "object",
  "additionalProperties": false,
  "required": ["groups", "_self_evaluation"],
  "properties": {
    "groups": {
      "type": "array",
      "description": "Grupos propuestos de incidentes que evidencian el mismo patrón conductual",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["signal", "incident_ids", "rationale"],
        "properties": {
          "signal": {
            "type": "string",
            "description": "Una frase corta que CAPTURA lo que todos estos incidentes tienen en común a nivel conductual — el PATRÓN SUBYACENTE al que apuntan estas diferentes expresiones. NO un {label_name}, solo una etiqueta descriptiva. Ej.: 'horas extra de trabajo', 'actuando para evaluadores'"
          },
          "incident_ids": {
            "type": "array",
            "items": {"type": "string"},
            "description": "IDs EXACTOS de los incidentes del input que evidencian este patrón. Mínimo 2."
          },
          "rationale": {
            "type": "string",
            "description": "Una oración explicando el PROCESO CONDUCTUAL SUBYACENTE que conecta estos incidentes, a pesar de sus diferencias superficiales"
          }
        }
      }
    },
    "_self_evaluation": {
      "type": "object",
      "properties": {
        "needs_retry": {
          "type": "boolean",
          "description": "true si consideras que tu output está incompleto, es de baja calidad, o requiere refinamiento adicional"
        },
        "retry_reason": {
          "type": ["string", "null"],
          "description": "SOLO si needs_retry=true. Instrucción CONCRETA y ACCIONABLE de qué mejorar en el próximo intento. Ej: 'Etiqueta demasiado genérica — especificar propiedades distintivas', 'Falta evidencia del segmento 47', 'La definición mezcla dos conceptos distintos — separar'"
        },
        "suggested_action": {
          "type": "string",
          "enum": ["proceed", "retry", "escalate_to_hitl", "skip", "abort"],
          "description": "Acción recomendada: proceed (output correcto, avanzar), retry (reintentar con feedback), escalate_to_hitl (decisión humana necesaria), skip (saltar este agente, no es relevante), abort (error irrecuperable, detener todo)"
        }
      },
      "required": ["needs_retry", "suggested_action"]
    }
  }
}
