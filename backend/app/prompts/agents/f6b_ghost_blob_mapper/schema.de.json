{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mappings"
  ],
  "properties": {
    "mappings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "memo_id",
          "disposition"
        ],
        "properties": {
          "memo_id": {
            "type": "string"
          },
          "disposition": {
            "type": "string",
            "enum": [
              "densify_existing",
              "suggest_new",
              "unmapped"
            ],
            "description": "Was mit diesem Memo zu tun ist."
          },
          "target_category_ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "IDs der zu verdichtenden Kategorien. Leer wenn disposition != densify_existing."
          },
          "what_it_adds": {
            "type": "string",
            "description": "Welche Eigenschaft, Dimension oder Variante dieses Memo zur Zielkategorie hinzufügen würde."
          },
          "suggested_new_category_name": {
            "type": "string",
            "description": "Name in Gerundium-Form für die neue Kategorie. Nur wenn disposition = suggest_new."
          },
          "suggested_new_category_definition": {
            "type": "string",
            "description": "Anfangsdefinition für die neue Kategorie. Nur wenn disposition = suggest_new."
          },
          "unmapped_reason": {
            "type": "string",
            "description": "Warum es nicht zugeordnet werden konnte. Nur wenn disposition = unmapped."
          }
        }
      }
    }
  }
}
