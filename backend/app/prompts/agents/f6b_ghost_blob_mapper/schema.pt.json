{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mappings"
  ],
  "properties": {
    "mappings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "memo_id",
          "disposition"
        ],
        "properties": {
          "memo_id": {
            "type": "string"
          },
          "disposition": {
            "type": "string",
            "enum": [
              "densify_existing",
              "suggest_new",
              "unmapped"
            ],
            "description": "O que fazer com este memo."
          },
          "target_category_ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "IDs das categorias a densificar. Vazio se disposition != densify_existing."
          },
          "what_it_adds": {
            "type": "string",
            "description": "Qual propriedade, dimensão ou variante este memo adicionaria à categoria alvo."
          },
          "suggested_new_category_name": {
            "type": "string",
            "description": "Nome em forma de gerúndio para a nova categoria. Apenas se disposition = suggest_new."
          },
          "suggested_new_category_definition": {
            "type": "string",
            "description": "Definição inicial para a nova categoria. Apenas se disposition = suggest_new."
          },
          "unmapped_reason": {
            "type": "string",
            "description": "Por que não pôde ser mapeado. Apenas se disposition = unmapped."
          }
        }
      }
    }
  }
}
