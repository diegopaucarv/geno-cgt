{
  "type": "object",
  "properties": {
    "core_category_candidates": {
      "type": "array",
      "description": "Candidatos a categoria central, ordenados por theoretical_grab decrescente",
      "items": {
        "type": "object",
        "required": [
          "code_id",
          "code_label",
          "why_central",
          "theoretical_grab"
        ],
        "properties": {
          "code_id": {
            "type": "string",
            "description": "UUID do código ou categoria candidato"
          },
          "code_label": {
            "type": "string",
            "description": "Rótulo atual do código"
          },
          "why_central": {
            "type": "string",
            "description": "Raciocínio qualitativo: por que este código emerge como candidato central. Deve referenciar os 5 critérios."
          },
          "relation_to_core_pattern": {
            "type": "string",
            "enum": [
              "is_the_core",
              "processes",
              "conditions",
              "consequences",
              "strategies"
            ],
            "description": "Tipo de relação com o {object_of_study} central"
          },
          "theoretical_grab": {
            "type": "string",
            "enum": [
              "High",
              "Medium",
              "Low"
            ],
            "description": "Poder explicativo e unificador do candidato"
          },
          "connected_code_ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "UUIDs de códigos que se conectam a este candidato"
          },
          "connected_code_count": {
            "type": "integer",
            "description": "Número de códigos conectados"
          },
          "limitations": {
            "type": "string",
            "description": "Quais aspectos do sistema de códigos este candidato NÃO explica bem"
          }
        }
      }
    },
    "no_core_detected": {
      "type": "boolean",
      "description": "true se nenhum código atual atinge o nível de categoria central"
    },
    "no_core_rationale": {
      "type": "string",
      "description": "Se no_core_detected=true: explicação do que falta nos dados"
    }
  },
  "required": [
    "core_category_candidates"
  ]
}
