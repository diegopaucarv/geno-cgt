{
  "type": "object",
  "required": [
    "all_valid",
    "issues"
  ],
  "properties": {
    "all_valid": {
      "type": "boolean",
      "description": "true si todos los códigos pasan la revisión."
    },
    "issues": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "code_name",
          "problem",
          "suggestion"
        ],
        "properties": {
          "code_name": {
            "type": "string",
            "description": "Nombre del código con problemas."
          },
          "problem": {
            "type": "string",
            "enum": [
              "vague_definition",
              "overlap",
              "not_grounded",
              "missing_properties"
            ],
            "description": "Tipo de problema."
          },
          "suggestion": {
            "type": "string",
            "description": "Cómo corregirlo. Una oración concreta."
          }
        }
      }
    }
  }
}
