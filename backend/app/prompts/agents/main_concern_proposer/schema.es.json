{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "core_concern",
    "recurring_problems",
    "resolving_mechanisms"
  ],
  "properties": {
    "core_concern": {
      "type": "string",
      "description": "{object_of_study} central expresado según estilo de codificación o frase verbal."
    },
    "rationale": {
      "type": "string",
      "description": "Razonamiento cualitativo conectando las 3 preguntas. Citar códigos específicos."
    },
    "recurring_problems": {
      "type": "array",
      "description": "{object_of_study}s recurrentes identificados (Pregunta 1). Array vacío si no se identificó ninguno.",
      "items": {
        "type": "string"
      }
    },
    "resolving_mechanisms": {
      "type": "array",
      "description": "Códigos o mecanismos que resuelven los problemas (Pregunta 2). Array vacío si no se identificó ninguno.",
      "items": {
        "type": "string"
      }
    },
    "most_connected_codes": {
      "type": "array",
      "description": "Códigos con mayor centralidad y poder explicativo (Pregunta 3). Array vacío si no se identificó ninguno.",
      "items": {
        "type": "string"
      }
    },
    "confidence": {
      "type": "string",
      "enum": [
        "HIGH",
        "MEDIUM",
        "LOW"
      ],
      "description": "Confianza en el {object_of_study} identificado."
    },
    "alternative_concerns": {
      "type": "array",
      "description": "Preocupaciones alternativas plausibles si la confianza no es HIGH.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "concern",
          "why_less_likely"
        ],
        "properties": {
          "concern": {
            "type": "string",
            "description": "Preocupación alternativa."
          },
          "why_less_likely": {
            "type": "string",
            "description": "Por qué es menos probable que la principal."
          }
        }
      }
    },
    "no_clear_concern": {
      "type": "boolean",
      "description": "true si los datos no respaldan un {object_of_study} claro."
    },
    "no_concern_rationale": {
      "type": "string",
      "description": "Si no_clear_concern=true: qué falta en los datos para identificar un {object_of_study}."
    },
    "relevant_population_dimensions": {
      "type": "array",
      "description": "Dimensiones poblacionales relevantes para entender cómo se manifiesta este {object_of_study}. Derivadas de A1 y prime movers. MOMENTO 1 de emergencia de variables.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "dimension_name",
          "observed_values",
          "emergence_rationale"
        ],
        "properties": {
          "dimension_name": {
            "type": "string"
          },
          "observed_values": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "emergence_rationale": {
            "type": "string"
          },
          "missing_values": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      }
    }
  }
}
