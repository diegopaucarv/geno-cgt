{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "matches"
  ],
  "properties": {
    "matches": {
      "type": "array",
      "description": "Segmente, die die Eigenschaft am gesuchten Extrem manifestieren. Leer wenn keine.",
      "items": {
        "type": "object",
        "required": [
          "segment_id",
          "exact_quote",
          "relevance"
        ],
        "properties": {
          "segment_id": {
            "type": "string",
            "description": "UUID des Segments."
          },
          "exact_quote": {
            "type": "string",
            "description": "Erste 200 Wörter des Segments, wörtlich."
          },
          "relevance": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 1.0,
            "description": "Wie deutlich es die Eigenschaft am gesuchten Extrem manifestiert. 0=keine, 1=eindeutig."
          }
        }
      }
    }
  }
}
