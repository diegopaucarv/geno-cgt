{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "action",
    "rationale",
    "essence_shared"
  ],
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "ENRICH",
        "SUBDIVIDE",
        "DIVIDE",
        "INSUFFICIENT_DATA"
      ],
      "description": "Maßnahme gemäß dem 3-Schritt-Protokoll entschieden."
    },
    "rationale": {
      "type": "string",
      "description": "Begründung, die die Protokollschritte durchläuft und spezifische Incidente zitiert."
    },
    "essence_shared": {
      "type": "boolean",
      "description": "Ergebnis von Schritt 1: true wenn die Gruppen eine zentrale Essenz teilen."
    },
    "new_property": {
      "type": "string",
      "description": "Nur wenn ENRICH. Neue hinzuzufügende Eigenschaft/Dimension. Leerer String wenn nicht zutreffend."
    },
    "subcategories": {
      "type": "array",
      "description": "Nur wenn SUBDIVIDE. Vorgeschlagene Subkategorien oder Gradientenanker.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "label",
          "description"
        ],
        "properties": {
          "label": {
            "type": "string",
            "description": "Name der Subkategorie oder des Ankers."
          },
          "description": {
            "type": "string",
            "description": "Welche Incidente zu dieser Subkategorie gehören."
          },
          "is_discrete": {
            "type": "boolean",
            "description": "true wenn diskreter Typ, false wenn Gradientenanker."
          }
        }
      }
    },
    "divided_categories": {
      "type": "array",
      "description": "Nur wenn DIVIDE. Vorgeschlagene neue Kategorien.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "definition"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Label der neuen Kategorie."
          },
          "definition": {
            "type": "string",
            "description": "Definition der neuen Kategorie."
          },
          "incident_ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "IDs der zugewiesenen Incidente."
          }
        }
      }
    }
  }
}
