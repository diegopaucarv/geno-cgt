{
  "type": "object",
  "additionalProperties": false,
  "required": ["issues"],
  "properties": {
    "issues": {
      "type": "array",
      "description": "Problems found with the label. Empty array if the label is good.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["label", "description", "suggestion"],
        "properties": {
          "label": {
            "type": "string",
            "description": "Name of the evaluated label"
          },
          "description": {
            "type": "string",
            "description": "What is wrong with the label"
          },
          "suggestion": {
            "type": "string",
            "description": "How to fix the issue"
          }
        }
      }
    },
    "conceptually_fitted": {
      "type": "boolean",
      "description": "True if the label is conceptually complete and no further refinement is needed. This ends the refinement loop early."
    },
    "referenced_entities": {
      "type": "object",
      "description": "Entities from the database that were referenced in this analysis. Populated by the orchestration layer for traceability.",
      "properties": {
        "segment_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Segment IDs referenced in this output."
        },
        "code_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Category/code IDs referenced in this output."
        },
        "hypothesis_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Hypothesis IDs referenced in this output."
        },
        "incident_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Incident IDs referenced in this output."
        },
        "document_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Document IDs referenced in this output."
        }
      }
    }
  }
}
