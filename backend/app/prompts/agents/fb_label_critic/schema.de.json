{
  "type": "object",
  "additionalProperties": false,
  "required": ["issues"],
  "properties": {
    "issues": {
      "type": "array",
      "description": "Gefundene Probleme mit dem Label. Leeres Array wenn das Label gut ist.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["label", "description", "suggestion"],
        "properties": {
          "label": {
            "type": "string",
            "description": "Name des bewerteten Labels"
          },
          "description": {
            "type": "string",
            "description": "Was ist falsch mit dem Label"
          },
          "suggestion": {
            "type": "string",
            "description": "Wie das Problem behoben werden kann"
          }
        }
      }
    },
    "conceptually_fitted": {
      "type": "boolean",
      "description": "True wenn das Label konzeptionell vollständig ist und keine weitere Verfeinerung benötigt wird. Dies beendet die Verfeinerungsschleife vorzeitig."
    },
    "referenced_entities": {
      "type": "object",
      "description": "Entities from the database that were referenced in this analysis. Populated by the orchestration layer for traceability.",
      "properties": {
        "segment_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Segment IDs referenced in this output."
        },
        "code_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Category/code IDs referenced in this output."
        },
        "hypothesis_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Hypothesis IDs referenced in this output."
        },
        "incident_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Incident IDs referenced in this output."
        },
        "document_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Document IDs referenced in this output."
        }
      }
    }
  }
}
