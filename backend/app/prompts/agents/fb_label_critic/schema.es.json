{
  "type": "object",
  "additionalProperties": false,
  "required": ["issues"],
  "properties": {
    "issues": {
      "type": "array",
      "description": "Problemas encontrados con la etiqueta. Array vacío si la etiqueta es buena.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["label", "description", "suggestion"],
        "properties": {
          "label": {
            "type": "string",
            "description": "Nombre de la etiqueta evaluada"
          },
          "description": {
            "type": "string",
            "description": "Qué está mal con la etiqueta"
          },
          "suggestion": {
            "type": "string",
            "description": "Cómo corregir el problema"
          }
        }
      }
    },
    "conceptually_fitted": {
      "type": "boolean",
      "description": "True si la etiqueta es conceptualmente completa y no necesita más refinamiento. Esto termina el loop de refinamiento temprano."
    },
    "referenced_entities": {
      "type": "object",
      "description": "Entities from the database that were referenced in this analysis. Populated by the orchestration layer for traceability.",
      "properties": {
        "segment_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Segment IDs referenced in this output."
        },
        "code_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Category/code IDs referenced in this output."
        },
        "hypothesis_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Hypothesis IDs referenced in this output."
        },
        "incident_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Incident IDs referenced in this output."
        },
        "document_ids": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Document IDs referenced in this output."
        }
      }
    }
  }
}
