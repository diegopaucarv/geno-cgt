{
  "type": "object",
  "additionalProperties": false,
  "required": ["unified_categories", "_self_evaluation"],
  "properties": {
    "unified_categories": {
      "type": "array",
      "description": "Vereinheitlichte, deduplizierte Kategorien nach Zusammenführung vorheriger und neuer.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["label", "definition", "source_categories", "merged_from", "action"],
        "properties": {
          "label": {
            "type": "string",
            "description": "Endgültige Bezeichnung für die vereinheitlichte Kategorie ({label_name} bevorzugt)."
          },
          "definition": {
            "type": "string",
            "description": "Kombinierte Definition, die alle Quellkategorien abdeckt."
          },
          "source_categories": {
            "type": "array",
            "items": { "type": "string" },
            "description": "IDs aller Kategorien, die zu dieser vereinheitlichten Kategorie beigetragen haben."
          },
          "merged_from": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Bezeichnungen der Quellkategorien (zur menschlichen Nachverfolgbarkeit)."
          },
          "action": {
            "type": "string",
            "enum": ["keep", "merge", "rename"],
            "description": "keep: unverändert | merge: 2+ Kategorien kombinieren | rename: Bezeichnung geändert, Definition beibehalten oder verfeinert."
          }
        }
      }
    },
    "_self_evaluation": {
      "type": "object",
      "properties": {
        "needs_retry": {
          "type": "boolean",
          "description": "true, wenn du der Meinung bist, dass deine Ausgabe unvollständig, von geringer Qualität ist oder zusätzliche Verfeinerung benötigt"
        },
        "retry_reason": {
          "type": ["string", "null"],
          "description": "NUR wenn needs_retry=true. KONKRETE und UMSETZBARE Anweisung, was beim nächsten Versuch verbessert werden soll. Z.B.: 'Label zu allgemein — spezifische Eigenschaften angeben', 'Fehlende Evidenz aus Segment 47', 'Definition vermischt zwei verschiedene Konzepte — trennen'"
        },
        "suggested_action": {
          "type": "string",
          "enum": ["proceed", "retry", "escalate_to_hitl", "skip", "abort"],
          "description": "Empfohlene Aktion: proceed (Ausgabe korrekt, fortfahren), retry (mit Feedback wiederholen), escalate_to_hitl (menschliche Entscheidung erforderlich), skip (diesen Agenten überspringen, nicht relevant), abort (nicht behebbarer Fehler, alles stoppen)"
        }
      },
      "required": ["needs_retry", "suggested_action"]
    }
  }
}
