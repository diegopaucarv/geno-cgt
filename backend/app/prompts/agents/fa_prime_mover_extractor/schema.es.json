{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "prime_mover",
    "confidence"
  ],
  "properties": {
    "prime_mover": {
      "type": "string",
      "description": "Patrón central expresado según estilo de codificación."
    },
    "description": {
      "type": "string",
      "description": "Descripción narrativa (2-3 oraciones) de cómo este patrón se manifiesta en el documento."
    },
    "evidence_quotes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Citas textuales de baseline_data que respaldan el prime mover."
    },
    "confidence": {
      "type": "string",
      "enum": [
        "HIGH",
        "MEDIUM",
        "LOW"
      ],
      "description": "Confianza en la extracción."
    },
    "insufficient_data": {
      "type": "boolean",
      "description": "true si no hay suficiente baseline_data para extraer un prime mover."
    },
    "alternative_patterns": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Patrones alternativos plausibles si la confianza no es HIGH."
    }
  }
}
