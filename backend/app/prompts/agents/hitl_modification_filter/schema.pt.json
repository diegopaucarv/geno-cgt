{
  "type": "object",
  "required": [
    "valid",
    "reason"
  ],
  "properties": {
    "valid": {
      "type": "boolean",
      "description": "true se a solicitação for do tipo aceito por este agente"
    },
    "reason": {
      "type": "string",
      "description": "Explicação em 1-2 frases"
    },
    "suggested_questions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Apenas se valid=false. Perguntas alternativas que este agente aceitaria."
    }
  }
}
