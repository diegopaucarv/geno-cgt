{
  "type": "object",
  "required": [
    "queries"
  ],
  "properties": {
    "queries": {
      "type": "array",
      "maxItems": 4,
      "items": {
        "type": "object",
        "required": [
          "type",
          "description"
        ],
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "rag",
              "code_lookup",
              "compare",
              "similar_codes"
            ],
            "description": "Art der Abfrage"
          },
          "description": {
            "type": "string",
            "description": "Wonach diese Abfrage sucht"
          },
          "text": {
            "type": "string",
            "description": "Nur für type=rag. Suchtext."
          },
          "code_id": {
            "type": "string",
            "description": "Nur für type=code_lookup. UUID des Codes."
          },
          "text_a": {
            "type": "string",
            "description": "Nur für type=compare. Erster Text."
          },
          "text_b": {
            "type": "string",
            "description": "Nur für type=compare. Zweiter Text."
          },
          "code_definition": {
            "type": "string",
            "description": "Nur für type=similar_codes. Zu vergleichende Definition."
          }
        }
      }
    }
  }
}
