{
  "type": "object",
  "required": [
    "valid",
    "reason"
  ],
  "properties": {
    "valid": {
      "type": "boolean",
      "description": "true wenn die Anfrage vom Typ ist, den dieser Agent akzeptiert"
    },
    "reason": {
      "type": "string",
      "description": "Erklärung in 1-2 Sätzen"
    },
    "suggested_questions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Nur wenn valid=false. Alternative Fragen, die dieser Agent akzeptieren würde."
    }
  }
}
