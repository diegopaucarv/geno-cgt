{
  "type": "object",
  "required": [
    "valid",
    "reason"
  ],
  "properties": {
    "valid": {
      "type": "boolean",
      "description": "true si la solicitud es del tipo aceptado por este agente"
    },
    "reason": {
      "type": "string",
      "description": "Explicación en 1-2 oraciones"
    },
    "suggested_questions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Solo si valid=false. Preguntas alternativas que este agente aceptaría."
    }
  }
}
