{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name_is_adequate",
    "suggestions"
  ],
  "properties": {
    "name_is_adequate": {
      "type": "boolean",
      "description": "true si el nombre actual es adecuado y no se sugiere renombrar."
    },
    "adequacy_rationale": {
      "type": "string",
      "description": "Si name_is_adequate=true: por qué el nombre actual sigue siendo bueno."
    },
    "suggestions": {
      "type": "array",
      "description": "Sugerencias de renombramiento. Array vacío si name_is_adequate=true.",
      "items": {
        "type": "object",
        "required": [
          "name",
          "level",
          "rationale",
          "what_it_gains"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Nombre sugerido según estilo de codificación."
          },
          "level": {
            "type": "string",
            "enum": [
              "conservative",
              "moderate",
              "transformative"
            ],
            "description": "Nivel de abstracción del renombramiento."
          },
          "rationale": {
            "type": "string",
            "description": "Por qué este nombre es mejor."
          },
          "what_it_gains": {
            "type": "string",
            "description": "Qué dimensión o propiedad captura que el nombre actual omite."
          },
          "in_vivo_inspiration": {
            "type": "string",
            "description": "Si el nombre está inspirado en palabras exactas de un participante, citarlas aquí. Cadena vacía si no."
          }
        }
      }
    }
  }
}
