{
  "type": "object",
  "additionalProperties": false,
  "required": ["spatial_frame", "temporal_frame", "generalized_population"],
  "properties": {
    "spatial_frame": {
      "type": "string",
      "enum": ["cohabiting_group", "sparse", "high_diversity"],
      "description": "Wie die Populationsmitglieder räumlich verteilt sind."
    },
    "temporal_frame": {
      "type": "string",
      "enum": ["present_continuous", "retrospective", "prospective", "longitudinal"],
      "description": "Zeitrahmen der untersuchten Population."
    },
    "generalized_population": {
      "type": "string",
      "description": "2-3 Sätze abstrakte Verallgemeinerung der Population, ohne spezifische Orte/Namen/Zahlen."
    },
    "rationale": {
      "type": "string",
      "description": "Kurze Begründung für die gewählten Frames."
    }
  }
}
