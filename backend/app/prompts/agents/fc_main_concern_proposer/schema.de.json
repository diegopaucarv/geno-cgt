{
  "type": "object",
  "additionalProperties": false,
  "required": ["candidates", "rationale"],
  "properties": {
    "candidates": {
      "type": "array",
      "description": "2-4 Kandidaten für Interessensmuster, jeweils als Gerundium ausgedrückt.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "statement",
          "supporting_codes",
          "orphan_codes",
          "rationale"
        ],
        "properties": {
          "statement": {
            "type": "string",
            "description": "Gerundium-Kandidat für das Interessensmuster."
          },
          "supporting_codes": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Kategorien, die diesen Kandidaten als Kernanliegen stützen."
          },
          "orphan_codes": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Kategorien, die verwaist wären, wenn dieser Kandidat gewählt wird."
          },
          "rationale": {
            "type": "string",
            "description": "Warum dieser Kandidat aus den Daten hervorgeht. Spezifische Hypothesen-IDs zitieren."
          },
          "is_latent": {
            "type": "boolean",
            "description": "true wenn es sich um ein latentes Anliegen handelt (durch Verhalten offenbart, nicht explizit geäußert)."
          }
        }
      }
    },
    "rationale": {
      "type": "string",
      "description": "Gesamtbegründung, die die Kandidaten mit den gesammelten Daten verbindet."
    },
    "no_clear_concern": {
      "type": "boolean",
      "description": "true wenn die Daten kein klares Interessensmuster stützen."
    },
    "no_concern_rationale": {
      "type": "string",
      "description": "Wenn no_clear_concern=true: was fehlt."
    }
  }
}
