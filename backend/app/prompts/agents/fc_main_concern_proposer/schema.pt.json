{
  "type": "object",
  "additionalProperties": false,
  "required": ["candidates", "rationale"],
  "properties": {
    "candidates": {
      "type": "array",
      "description": "2-4 padrões de interesse candidatos, cada um expresso como {label_name}.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "statement",
          "supporting_codes",
          "orphan_codes",
          "rationale"
        ],
        "properties": {
          "statement": {
            "type": "string",
            "description": "Candidato em {label_name} para o padrão de interesse."
          },
          "supporting_codes": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Categorias que respaldam este candidato como a preocupação central."
          },
          "orphan_codes": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Categorias que ficariam órfãs se este candidato for escolhido."
          },
          "rationale": {
            "type": "string",
            "description": "Por que este candidato emerge dos dados. Citar IDs de hipóteses específicos."
          },
          "is_latent": {
            "type": "boolean",
            "description": "true se é uma preocupação latente (revelada pelo comportamento, não declarada explicitamente)."
          }
        }
      }
    },
    "rationale": {
      "type": "string",
      "description": "Raciocínio geral conectando os candidatos aos dados acumulados."
    },
    "no_clear_concern": {
      "type": "boolean",
      "description": "true se os dados não respaldam nenhum padrão de interesse claro."
    },
    "no_concern_rationale": {
      "type": "string",
      "description": "Se no_clear_concern=true: o que está faltando."
    }
  }
}
