{
  "type": "object",
  "additionalProperties": false,
  "required": ["candidates", "rationale"],
  "properties": {
    "candidates": {
      "type": "array",
      "description": "2-4 patrones de interés candidatos, cada uno expresado como gerundio.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "statement",
          "supporting_codes",
          "orphan_codes",
          "rationale"
        ],
        "properties": {
          "statement": {
            "type": "string",
            "description": "Candidato en gerundio para el patrón de interés."
          },
          "supporting_codes": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Categorías que respaldan a este candidato como la preocupación central."
          },
          "orphan_codes": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Categorías que quedarían huérfanas si se elige este candidato."
          },
          "rationale": {
            "type": "string",
            "description": "Por qué este candidato emerge de los datos. Citar IDs de hipótesis específicos."
          },
          "is_latent": {
            "type": "boolean",
            "description": "true si es una preocupación latente (revelada por el comportamiento, no declarada explícitamente)."
          }
        }
      }
    },
    "rationale": {
      "type": "string",
      "description": "Razonamiento general conectando los candidatos con los datos acumulados."
    },
    "no_clear_concern": {
      "type": "boolean",
      "description": "true si los datos no respaldan ningún patrón de interés claro."
    },
    "no_concern_rationale": {
      "type": "string",
      "description": "Si no_clear_concern=true: qué falta."
    }
  }
}
